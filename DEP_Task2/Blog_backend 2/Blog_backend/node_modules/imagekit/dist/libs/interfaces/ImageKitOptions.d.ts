import { TransformationPosition } from "imagekit/dist/libs/interfaces";
export interface ImageKitOptions {
    uploadEndpoint?: string;
    publicKey: string;
    privateKey: string;
    urlEndpoint: string;
    transformationPosition?: TransformationPosition;
}
